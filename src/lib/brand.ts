/**
 * The one credit line the product uses, everywhere it appears: reports, the
 * landing page footer and outgoing email. The customer's own organisation name
 * and logo are the prominent branding on a report — ours is a discreet credit
 * at the foot of the page, never a banner across the top.
 */
export const BRAND_CREDIT = "instructBrain — An instructSite Company";

/** Plain-text form for places that cannot carry an em dash safely. */
export const BRAND_CREDIT_PLAIN = "instructBrain - An instructSite Company";
